from setuptools import setup

setup(
    name='oms-client',
    version='0.0.1',
    scripts=[],
    packages=['omsclient'],
    include_package_data=True,
    install_requires=[
        'requests'
    ]
)

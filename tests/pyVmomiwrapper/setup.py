from setuptools import setup

setup(
    name='pyVmomiwrapper',
    version='0.0.3',
    scripts=[],
    packages=['pyVmomiwrapper'],
    include_package_data=True,
    install_requires=[
        'pyvmomi>=6.0.0'
    ]
)

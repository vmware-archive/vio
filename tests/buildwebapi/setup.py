from setuptools import setup

setup(
    name='buildwebapi',
    version='0.0.2',
    scripts=[],
    packages=['buildwebapi'],
    include_package_data=True,
    install_requires=[
        'requests'
    ]
)
